Config = {} 

Config.Standalone = false --[[ if its standalone it will only use FiveM natives that are documentated here: https://docs.fivem.net/natives/ 
                             else it will use esx to register the command and notify the player(s) ]] -- its ESX or Standalone xD
                            